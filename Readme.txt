This is a simple rss reader right now.
It just takes the yahoo weather rss feed and parses it to grab the five day forcast

Hopefully it will eventually get to the point where it will pass that data to a 16x2 
lcd screen hooked up to a beagle bone black
